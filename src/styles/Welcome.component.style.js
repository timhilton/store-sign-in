import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    height: 600,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
